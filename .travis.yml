language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"

install:
 - pip install pytest-cov
 - pip install -r requirements.txt
 - pip install .


script:
  - pytest --cov=dummy

after_success:
 - bash <(curl -s https://codecov.io/bash)
